name: Test Check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run test check
        run: |
          echo "Running test check..."
          echo "Test check passed!"
